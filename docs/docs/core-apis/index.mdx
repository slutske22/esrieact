# CORE APIs

The ArcGIS JS API is _extensive_. Currently, there is not an esrieact correlary for every ArcGIS JS API class and component. However, the core esrieact APIs will enable you to create react comopnents to handle most use cases.
